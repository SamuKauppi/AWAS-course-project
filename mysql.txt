DROP DATABASE IF EXISTS awas_db;
CREATE DATABASE awas_db;
USE awas_db;

DROP TABLE IF EXISTS users;
CREATE TABLE users (
    username VARCHAR(50) NOT NULL PRIMARY KEY,
    password VARCHAR(255) NOT NULL,
    money DECIMAL(15, 2) NOT NULL DEFAULT 0.00
);
